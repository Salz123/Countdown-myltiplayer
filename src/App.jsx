export default function App() {
  return (
    <div style={{ padding: 40 }}>
      <h1>✅ React is running</h1>
      <button
        onClick={() => alert("Button works")}
        style={{ fontSize: 18 }}
      >
        Test Button
      </button>
    </div>
  );
}